# GameWeek

## Controls:

# movement
`w` move forward  
`s` move back  
`a` turn clockwise  
`d` turn counterclockwise  

# weapons
`j` fireburst  
'click' fireball  

# settings
`m` toggle map  
`g` toggle grid  
